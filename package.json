{
  "name": "midnight-loadboard",
  "version": "0.1.0",
  "private": true,
  "description": "Free load board and driver coverage verification for owner-operators and small carriers.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "scripts": {
    "dev": "http-server . -p 8080"
  }
}
